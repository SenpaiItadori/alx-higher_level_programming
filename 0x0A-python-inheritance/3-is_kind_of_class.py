#!/usr/bin/python3

"""Defines a function that sees if an object is an instance of a class."""


def is_kind_of_class(obj, a_class):
    """Return True is the object is instance of a_class."""

    return (isinstance(obj, a_class))
